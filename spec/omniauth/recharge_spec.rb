require "spec_helper"

describe Omniauth::Recharge do
  it "has a version number" do
    expect(Omniauth::Recharge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
